const Playground = () => {
  return (
    <section
      id="playground"
      className="mx-auto my-4 flex h-full flex-1 flex-col overflow-hidden"
    >
      A Place to mess around and build components
    </section>
  );
};

export default Playground;
